# Alpaca paper trading credentials — needed starting Day 8, not before.
# Never commit the real .env file (it's gitignored).
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
APCA_API_BASE_URL=https://paper-api.alpaca.markets
